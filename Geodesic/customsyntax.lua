-- Custom Syntax:

custom_syntax = { -- The custom syntax uses luas pattern matching characters, so you can do stuff like "(any text here)"" to be a specific thing
    ["EXAMPLE CUSTOM SYNTAX"] = {ishexpattern = false},
}